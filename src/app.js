const React = require('react');

const App = React.createClass({
    render: function () {
        return (
            <div>
                HELLO WORLD!
                </div>
        )
    }
});

module.exports = App;